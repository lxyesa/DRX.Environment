@page
@model NDV_WebASP.Pages.IndexModel

@{
    
}

<body>
    <style>
        .container {
            background-color: #f9f9f9;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh; /* 使容器高度占满整个视口 */
        }

        .sign-in-box {
            max-width: 440px;
            width: calc(100% - 40px);
            padding: 44px;
            background-color: #fff;
            box-shadow: 0 2px 6px rgba(0,0,0,0.2);
            min-width: 320px;
            min-height: 338px;
            overflow: hidden;
        }
    </style>

    <div class="container">
        <div class="sign-in-box">
            <h1>DRX 账户 | 登录</h1>
            <p>页面未找到</p>
            <form method="post">
                <button type="submit" asp-page-handler="SignIn">登录</button>
            </form>
        </div>
    </div>
</body>
