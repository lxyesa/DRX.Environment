# {类名}
- **抽象基类/接口**：是否为抽象基类或接口，以及其在架构中的作用。
- **功能描述**：该类/接口的主要功能和用途。
- **设计目的**：设计该类/接口的初衷和目标。
- **使用场景**：典型的应用场景和示例。

## 类型定义
- **类型参数**：如果有泛型，列出类型参数及其约束。
- **泛型约束**：对泛型参数的具体约束条件。
- **继承关系**：该类型继承自哪个基类。
- **实现接口**：实现的接口列表。

## 属性 {属性名称}
- **数据类型**：属性的数据类型。
- **访问修饰符**：如`public`、`private`等访问权限。
- **属性描述**：属性的具体描述和用途。
- **默认值**：属性的默认值（如果有）。
- **读写权限**：属性是只读、只写还是读写。

## 构造函数
- **构造函数签名**：构造函数的完整签名。
- **参数列表**：构造函数的参数，包括名称、类型和描述。
- **初始化逻辑**：构造函数中执行的初始化操作。
- **特殊说明**：如参数的默认值设置等特殊说明。

## 方法 {方法名称}
- **方法签名**：方法的完整签名，包括返回类型和参数。
- **返回类型**：方法返回值的数据类型。
- **参数**：方法的参数列表，包括名称、类型和描述。
- **方法描述**：方法的功能和行为描述。
- **功能实现**：方法实现的具体逻辑。
- **调用流程**：调用该方法时的流程和步骤。
- **异常抛出**：可能抛出的异常类型、条件和描述。
- **虚拟/抽象/静态修饰**：方法是否为虚拟、抽象或静态。
- **重载说明**：如果有重载，说明不同重载版本的区别。

## 事件
- {此处应该为一个表格}

## 示例代码
- **示例类/方法定义**：提供一个示例类或方法的定义。
- **属性/方法使用示例**：展示如何使用类的属性和方法。
- **完整示例流程**：一个完整的使用示例，包括步骤和流程。
- **代码注释**：代码中的注释，解释关键部分的逻辑。
- **输入/输出示例**：示例的输入和预期输出。